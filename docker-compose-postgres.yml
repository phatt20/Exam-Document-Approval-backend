# docker-compose.yml
version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: postgres-db-Document
    restart: always
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 115120
      POSTGRES_DB: Document
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
